<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Payment Failed</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #b0c4c4;
        }
        .container {
            text-align: center;
        }
        .alert-icon {
            position: relative;
            width: 100px;
            height: 100px;
            background-color: red;
            clip-path: polygon(50% 0%, 0% 100%, 100% 100%);
            margin: 0 auto 20px;
        }
        .alert-icon::before {
            content: '!';
            position: absolute;
            top: 20%;
            left: 50%;
            transform: translateX(-50%);
            color: white;
            font-size: 60px;
            font-weight: bold;
        }
        .alert-icon-border {
            position: absolute;
            top: -10px;
            left: -10px;
            right: -10px;
            bottom: -10px;
            border: 10px solid purple;
            clip-path: polygon(50% 0%, 0% 100%, 100% 100%);
            z-index: -1;
        }
        .message {
            color: navy;
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
        }
        .retry-btn {
            background-color: #4CAF50;
            border: none;
            color: white;
            padding: 15px 32px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin: 4px 2px;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }
        .retry-btn:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="alert-icon">
            <div class="alert-icon-border"></div>
        </div>
        <div class="message">order payment failed</div>
        <button class="retry-btn" onclick="retryPayment()">Retry Payment</button>
    </div>

    <script>
        function retryPayment() {
            window.location.href = "/user/getpaymentpage"
        }
    </script>
</body>
</html>